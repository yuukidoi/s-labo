         
         
        
        


<% if posts.any? %>
  <div class="row">
    <% posts.each do |post| %>
    　
         <div class="col-lg-3" id="card">
            <div class="card">
                <div class="card-header">
                     <%= image_tag post.image.url, :size =>'310x300' if post.image.url %>
                </div>
                <div class="card-body">
                    <p><%= post.image_comment %></p>
                </div>
            </div>
        </div> 
    <% end %>
  </div>
<% end %>

       