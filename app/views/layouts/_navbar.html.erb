<header class="header">
    <nav class="navbar navbar-expand-sm navbar-dark" style="background-color:#333333">
        <a class="navbar-brand offset-1" href="/" id="top">指導案Labo</a>
        
        
    <%= search_form_for @search do |f| %>
      <div class="actions">
        <%= f.label :title_or_goal__or_preparatioin_or_explanation_or_school_or_grade_or_subject_or_content_or_tags_name, '指導案を探す', :class => 'search_name_here' %>
        <%= f.search_field :title_or_goal_or_preparation_or_explanation_or_school_or_grade_or_subject_or_content_or_tags_name_cont %>
        
      <%= f.submit "検索" , :class => 'search' %></div>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collpase navbar-collapse justify-content-end" id ="navbarSupportedContent">
            <ul class="navbar-nav">
              <li class="nav-item"><%= link_to "ユーザー一覧", users_path, class: "nav-link" %></li>
            <% if logged_in? %>
            
            <li class="nav-item dropdown"></li>
            　 <li class="navbar"><%= link_to new_document_path, :class=> 'add', data: {"turbolinks" => false} do %>
            　 <i class="fas fa-file-upload", id="nav_icon"></i>投稿
            　 <% end %>
            　 </li>
               <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown"><i class="fas fa-user", id="nav_icon"></i><%= current_user.name %></a>
               <ul class ="dropdown-menu dropdown-menu-right">
                    <li class="nav-drop-item" ><%= link_to 'プロフィール', user_path(current_user.id) %></li> 
                    <li class="nav-drop-item" id="drop"><%= link_to 'ログアウト', logout_path, method: :delete %></li>       
                </ul>
            <% else %>
                <li class="nav-item"><%= link_to '登録', signup_path, class: 'nav-link' %></li>
                <li class="nav-item"><%= link_to 'ログイン', login_path, class: 'nav-link' %></li>
            <% end %>
                
            </ul>
        </div>
    </nav>
</header>